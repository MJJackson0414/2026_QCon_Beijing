# 逐字稿（校正版）：讓每個員工都有一個 Coding Agent —— 螞蟻 Vibe Coding 平台（Muse）落地半年後的實踐經驗

> 講者：**彭佩喬**（花名喬楊），技術專家，螞蟻集團 - 支付寶 - 體驗技術部
> 主題：**讓每個員工都有一個 Coding Agent：螞蟻集團 Vibe Coding 平台落地半年後的實踐經驗** — QCon 全球軟體開發大會（平台名：**Muse**）
> 校正說明：本篇依原始簡報 PDF（34 頁）逐頁核對，校正專有名詞、修正同音錯字、重新斷句分段，保留口語風格。文末附「主要校正對照」與「仍不確定之詞」。

---

## 開場：自我介紹與平台介紹

大家好，我是彭佩喬，我來自螞蟻集團（支付寶）體驗技術部，我的花名是喬楊。我今天來這裡，是因為我們在去年 7 月份左右，在內部（發布）了一款在線的、可視化的 coding 平台（**Muse**），到現在已經有了大半年的實踐時間。我們（踩）了很多真實的（坑）——平台在公司內部業務落地的實際的坑，也給大家分享一些我的心得體驗。

我先簡單快速地介紹一下我們在內部做的是一個什麼樣的平台：我們是用自然語言和 AI 對話、生成「前後端一（體）網站」的這樣一個平台。我們面向的是目前內部的所有員工，不需要他有任何（技術）背景，他只要會打字、會敲回車就行。我們最近還做了語音輸入的功能，所以現在（不）會打字、只會（敲）回車的（也）很方便。

我們可以做這麼四種類型的網（站）：第一種比較簡單，就不多說了，就是做一個簡單的靜態的前端網站；第二種是可以做「前端加上它自己的接口」——這個怎麼理解呢？在螞蟻內部有統一的 API 網關平台，我們所有的服務端系統寫的代碼、我們的接口都會自動註冊到這個網（關）。那當我想要使用某個系統的某個接口來做一個網站的時候，比如我寫了一個接口、想快速做一個功能豐富的網站，我只需要在我們的平台上把這個接口選中，我們就會自動把接口的出入參、它的元信息封裝成大模型特別好理解的上下（文），然後就可以直接讓大模型幫（我）完成網站——所以這個面向有自己服務端的場（景）。第三種是面向「非技術群體」的：他自己不懂技術，但他需要做一個（有）真實數據、真實服務的一個網站，那麼他只需要跟我們的平台自然語言對話，我們會自動幫他生成表、關聯關係、CRUD 接口，然後把它連起來、連上他的前端網站，做成這樣一個（應用）——這個是目前我們內部量最大的一種類型。（第四）種是最近特別火的：假設他第三種類型做完之後（比如做了一個項目管理平台），他想要再進一步給這個平台加一個「AI 助手」——用戶可以在他的網站上和它的 AI 對話完成功能——那麼我們其實內置了很多現在大模型最好的實現，比如怎麼調用模型、怎麼做流式渲染、怎麼做 Context Engineering、怎麼做它的（記憶）等，我們都是內置好的，他只要用自然語言描述，我們就可以給他搭一個（AI 助手）內嵌到他的網站裡面。

這是我們這個平台大（概）的介（紹）。簡單看：比如左邊那個圖，就是在我們平台上可以「一邊對話、一邊在網站上實時查看」——瀏覽器裡（跑）的（是）大（模）型（實時）寫（的頁面），比如這是一個 React，它可以（跟）你交互；也可以像傳統軟件一樣，你這樣選擇我一段比如文字，可以直接改文字的（排版）、文字的顏色。這是為了兼容上一個時代（比如低代碼時代、或傳統設計軟件時代）用戶的一些使用習慣——他有時候就是不想對話、簡單地改個字，可以就直接這麼改，我們會自動把這個改動封裝成一個大模型可以理解的上下（文），這樣（穩定）它的意圖、後續對話的時候不會丟失。以及他（可以）做多人協作——可以隨時生成一個預（覽）（鏈接）分享給別人，也可以和別人多人同時協作。

接（完）之後我就講真實使用場景。（補充）就是這個形態（在）內部其實接受度還是比較高的：我們做了半年時間，我們內部的 MAU 就已經過萬了，有一半是非（技術）群體在用，大概大家對話 12（輪）就可以做一個（網站）。下面我就以三個我真實的內部用戶使用 case，來跟大家講一下我們這大半年做的事情。

## 案例一：一位 PMO 同事 Vibe 了一個專屬「戰役管理平台」（2000+ 輪、6 億 tokens）

首先，有一天我一個比較熟的、一個 PMO 同（事）找我，他（說）「我最近老闆給我一個新的命題，我要做一個數字化管理平台，但現有的各種軟件都滿足不了我的定制（需）求。我聽說你們自己做 AI 的這東西，我能不能用？」我說「包（的），哥，我這就是給你們（非）技術（群）體的，你就大膽放心來，跟 AI 對話、你就敲回車給你做出來，你就敲起來。」然後這哥敲了兩週回車，燒了我六個（億 tokens）——他是我們第一個重度（用）（戶），第一個月就花了我們快 5000 塊的（賬單）。

花（這麼）多錢，但是用戶是沒有錯的，（費）錢不是用戶的問題、（費）錢是（我們）平台的問題。所以怎麼省錢？我就想怎麼省錢，錢花哪去了、為什麼這麼貴？（token）肯定是在代碼上花最多。當時是比較早、去年那時候，大家還沒有現在很多新的最佳實踐，我那時候怎麼改代碼呢？比如一個文件 2000 行代碼，我就把它整個（讀）給大模型，然後把（需求）給他，大模型改完返回來——這一來一回、（token）加（token）太多了。

那一個比較簡單的（優化）就是：我可以只給他他想要改的那部分。我給他提供一個叫 **Search（Replace）工具**，他找到（行）、找到他想要（改的）上下文對應的（行），改完之後再返（回），比如真改五行，「五行進、五行出」。你（想）（了）好多辦（法）——聽起來很簡單，但實（現）起來，前者很簡單、後者稍微麻煩一點。我們第一個想法是「怎麼精準修改一個文件」：業界已經有很成熟的工（具）——**git patch**，我改哪一行、哪一（列）、從什麼改成什麼，很簡單、都很成熟。但是我們讓大模型（生成）這個 patch，成功（率）（不到）80%，因為這個 patch 格式非常複雜、有很多字符、很多（格式）要求，（一）（旦）錯了這個 patch 就打不上（去）——輕則（沒）（打上），重則直接掛掉。（成功）率只有 80%（當時的模型）。

後來我們（想）了一種簡單一點的：既然 git patch 這個格式太複雜了，我們用簡單一點的，要不就「改哪一行、返（回）哪一行」吧——比如五行，你就把這五行全給我、告訴我行號，比如你改了第三行和第五行，你告訴我第三行換什麼、第五行換成什麼。成功率從 80% 幹到 90（%），還是不行。只要不是 100（%），我覺得都是不可以接受的。為什麼呢？因為大（模型）（算）（行）號會弄錯——我一個 2000 行的代碼，你跟他說改第 195 行，他（算）（的）（行）號會弄錯的。而且這個可怕的點還在於什麼呢？git patch 錯了就（炸）了、沒事、我知道；他（改）（錯）（行）改錯了、改到哪行去了我不知道呀，看不出來、沒報錯——可怕。這方案不行。

我們研究了很多、又參考了很多業界的其他方案，換一種——也是現在大家通（用）的方案：就是你把「原本要改的內容、新的內容」給我，比如原本我要改 A、A 要改成 A'、B'，你就把這（個）給我、我自己去 search 好了。稍微需要做一些工程化的優化，比如 A 出現了三次、我不知道給哪一個，我就在一次 **agent loop** 裡面告訴大（模型）「你再把上下文多給我一行，我（的）結果需要 narrow（收窄）一下、需要更（唯）（一），直到結果只有一個的時候，我才會真正去應用這個」。這是一種解法。類似的省（token 的）小技巧當（時）做（了）很多很多。

**但最近發現其實不需要（那麼多技巧）了。新的「版本答案」是什麼？—— Cache（KV Cache / Prompt Cache）。** 先給大家簡單介紹一下什麼是大模型的（Cache）：大模型做 GPU（計算）的時候，它並不是把你這些推理（結果）就扔了，它的計（算）（中間）結果可以緩存、可以緩存在 GPU 裡面，你可以後續用。後續怎麼用呢？—— **KV（Cache）**。我舉個例子：你和一個大模型對話應用，你說了 10 輪，再說第 11 輪，其實前面十輪的（計算/KV）不用完整地計算一遍——因為前面十輪已經發生了、第十輪的（內容）不會（變），那只要把第 11（輪）追加、（復用）之前的計算結果向量，重新再算一（點）就（好）。

比如 Anthropic（Claude）它就有兩個模式，它的最新（Prompt Cache）模式你可以（調）（用）（時）選「我要五分鐘的緩存、還是要一個小時的緩存」：你要五分鐘緩存，那我就給你存在顯（存）裡面，你下次命中我就給你打（折）——緩存命中之後打一折，非常非常便宜；你也可以要一個小時緩存，（那）可能存在（別的地方）、需要（重新）（召）回再算，價格不一（樣）。

回到我們這個怎麼用緩存呢？很簡單，在 coding 上（面）（天）生就特別適合。比如：**System Prompt** 這很簡單，我不要放任何動態內容（當年有很多人（放）（日期）、（放）（現）在（是）什麼時（刻）——不要搞這個邏輯，這種（東西）（一）（次）（對話）（裡）不（會）變）；一次對話裡面「人設」絕對不要變。主要是（工具/tools）——就是說我這次對話可以用哪些（工具）、（工具）的描述是什麼，這很簡單，你只要固化之後、我這次就（固）（定）（這）十個（工具），比如 read/write 這個（工具）我固定就不要改了。以及我要有一套 **stable（穩定）的序列化方法**，就是保證我只要是這個（工具），我（序）（列）化一定長得一樣（最（起碼）保證前置不（變））。第三個大頭就是 **repo map**——這是我這次的代碼倉庫，比如 Cursor 給倉庫建一個 code index：我有哪些文件、我的依賴是什麼、（結構）是什麼。這個東西如果你的項目沒有大的重構，這個內容也是不會改變的、也是 stable 的。這邊一看，大家看這過半的（部）分就已經不變了、剩下這部分就打一折，真的很便宜。

再往下 **opened files**：比如你當前正在改哪個文件，我就把這個文件讀到（context）裡面（這部分比如像我們可以（左）（右）（微調））。那麼你看，真正會變的（就是）用戶的 **user message**——也就是說，假設你在 Cursor 場景裡面對著一個文件當場連續對話、連續改，你每次（命）中（緩存）（的）（幾）（率）比較（高），真的很便宜。你只要連續對話，每次（都）命中同一台機子（同）一個（GPU），只要打中就很便宜。比如看這裡這個例子，覺得比較（奇）（怪）（說）1%，其實不是——其實真實（命）（中）（很）（高）、80% 了，80% 的（部分）很便宜。所以這是我覺得可能（顛覆）性的就是 **Prompt Cache** 小技巧。另外說一個 GitHub 上有個火（的）（項）（目）叫（「山斗人」/某壓縮 prompt 的項目），大家可能看過，他就是把大家的自然語言變成非常破碎、提升信息密（度），類似於文言文的方案——很多這種小技巧，以後可能都會被（Prompt Cache）（這種）（新）方案（取）（代）。這是我們一個（優化）。

**第二個問題（上下文壓縮）：** 這個（哥）有天說「我感覺你們 AI 傻了，他開始胡言亂語了、開始跟我講故事了、開始（忘）（了）自己（是）（誰）（了），怎麼辦？」——確實，他當時對話了快 1000（輪），確實頂不住了。有一個常見（問題）：任何大模型的上下文都是有限的，當時還是 200K（上下文），那 200K 塞都塞不下，肯定要壓縮。每個平台都有做壓縮，怎麼壓呢？比如 Claude 他們有七個原則（比如用戶怎麼放、代碼怎麼放等），他們（訓）（練）自己（的）一個（大模型）壓縮又快又好。但我想說：每一個垂類平台都應該自己研發自己的壓縮策略，因為只有你自己知道「壓縮之後你的信息丟哪些、保留哪些」，這是非常關鍵（的）。

比如我們平台，我現在是三層架構：第一層是最通用的 **memory**（這個和大家差不多，一個通用的東西）。但（有些場景）一定會（出問題）：比如我改了一個顏色，他突然（後面）說「我剛剛（那個）不好、能（不能）換」——那如果是 Claude 怎麼辦？這句 user message（可）能丟了，（那）他就會（不知道）「我剛剛那個什麼什麼」、AI 已經不知道了。所以怎麼辦呢？我們還加了一層 **user message 專門的一層（壓縮）**，專門放了（用戶的意圖）——user message 真的很短、真的很（省）（token），（用）（不）（了）（多少）token，（值得）放在（更）高（優先級）。第三個是重點：我們當時也是受到很多 **Manus**（啟發）——Manus 起（了）（一）個概念叫做「**文件即記憶**」，就是我每一個重要的、（正）（確）的執行結果我都應該（持）（久）（化）：每次壓縮之後，我把（要）壓縮的東西變成一個文件存在目錄裡面，把（索）引放到上下文裡面。比如我之前有一次 **WebSearch** 搜索了五個結果、我（只）展示三個，那用戶（後面）說「我之前搜索的第五個結果還想（用）」，那怎麼辦？傳統壓縮就丟了。所以怎麼壓縮不丟信息？就是「把記憶卸載掉、在合適的時候再加載」，這是一個很好的大模型的（理）（念）。另外就是 **Skills**——我這個項（目）其中哪些 Skills、我都把它以文件的形式放在當前目錄裡面（索）（引）一下，這樣這個 Skill 的（保）（持）（率）在當前項目對話裡面可以保持穩定。最後就是 coding，這個會（跟）（CLAUDE.md）差不多——這個（外）（層）定制一下我們項目裡面有哪些文件（一定）保（留）、（一）（定）不會丟。

這個給他緩解了一段時間。他過了幾天說「不行了、又不行了、太卡」，我一看——好傢伙，他一個完全不懂技術的項目管理人做了一個系統，70 張表、200 多（接口），（就是）（一）（個）（相當大的系統）。那既然（像）我剛說那個上下文壓（縮），其實可以不用一定要（等）觸發的時候才做，我們可以把它變成一個「常態化的能力」。最近大家（可能）也（看）（到）有個項目叫 MCP（-related，將 MCP 結果外置），這個項目火（了）——他說可以（節）省 90% 的上下（文）：他把 MCP 這個結果都外置了，類似於我們當時（的）理念。我們在每次（壓縮）（時）就把它不塞上下文、就塞在文件裡面，只把（索）引放在（context）裡面，大部（分）需要（時）大部（分）自己用（命令）的方式（讀）——為什麼這麼做？Linux、（bash）這些東西大模型是「一等公民」、是原生語言、是大模型最擅長的，你不用放在上下文，你把文件的地址給他，他自己會 search、自己會（cat/grep），他都會，他能很精準地找到他想要的內容，會保證這個記憶不會（被）壓（掉）——這個非常（關鍵）。

第二（點），（我）想（說）：在 AI 時代很多事情並不一定要強依賴 AI。我們是做工程的、做了這麼多年，我們有很多工程實踐。比如你讓大（模型）改一個函數叫 `getCurrentUserInfo`，那這個名字可能在每個文件都出現一次、（每個）文件都有五六個函數都叫這個名字，那（大模型）現在（要）一個一個（去）找哪一（個）要改、效率非常（低），怎麼辦？我們團隊可以提前給項目做一次編譯、做一次語法的（鏈）接、做一個（建）（模），我知道每個函數在哪裡定義、每個函數被誰引用、文件之間的關係是什麼——我會有一個「**有向無環圖**」來描述整個項目。當大（模型）想（改）某一個（函數）、想做一個（重）（構）的時候，對某個函數，他只要調用（工具）、我給他（返）（回），他（就）可（以）找到這個函數在（哪）被定義、被（誰）消費了，直接就（拿）（到）——這個基本上只用這個方式，效率高很多很多。所以這是我最近想分享（的）另（一）個經驗：**工程這些老的優化經驗在現在（AI）時代還是適用（的）**，你把你的能力（提供）（成）（工具）（給）（AI）好用。這個方式解決了它大（型）項目「改東西不准、以及找不到」的問（題）。

再往下他（還有）問題——我們當時的數據庫能力是以 GUI 的方式提供給他的，他自己定義他有哪些表（結構）他可能（弄）不明白。然後很簡單嘛，就聯合我們的 DB（團）隊，把我們所有能力（對齊）（業）界一個比較有名的 SDK 叫 **Supabase SDK**——我們完全對齊、完全給大模型（設）（置）一套 tool，讓大模型可以根據用戶的任何意圖來生成表、（改）表（字）（段）、修改表設計、（設計）表關聯關係、（設計）CRUD 接口。這裡（要）（提）（醒）的點就是：**企業內部造輪子一定要注意，你的編程界面需要（跟）（外）（界）（開）源的項目對齊**，不然（AI）不會寫——AI 會寫（開源的）、寫（你的）（私有的）就（廢了）。你不要再造私有（協議）、寫任何沒有（訓）（練）（過）（的）（東西）。現在我們（的）版本可以讓用戶通過對話建表、改表，而且我們給它每一個表結構都會生成一個 **snapshot** 版本，它可以任意回歸到任何一次表結構、而且數據保留——這是確保我們那些不懂技術的用戶不小心（改）錯了、改（動）了表結構什麼的，他（可以回退）；我們會給它記錄它的每個版本，（每次）我們都會讓（大模型）總結一條變化的 message（讓用戶）看懂。

但其實我想說：這個其實不是難題，對我們技術來說做得很簡單。難的是「怎麼啟動」。大家可以看這個「我要做的投票」——我們有一個 HR（同事），（他要做一個團建報名，）他（做）了一個（表單），但是他這個（存數據的）意圖那次沒有（喚）起、我也沒跟他檢查、我也不知道（沒存），他發（給）團隊收集，第二天說「你們（家）做（的）不行、數據沒有存下、怎麼用？」——我說這（才）是難題。就是「我要做投票」——（他其實需要）你要做一個 **localStorage** 版本的（存儲），我也不知道（他要什麼）、他也沒看出（來）（意圖）沒啟動，這怎麼辦？所以我想說：**今天用一個（Cache）（也好），今天 prompting 依然是一個非常非常關鍵的經驗（值得學）**，還是值得非常好地（雕）（琢）——我們這個（prompt）雕了很久很久，保障（改）（動）不會太大、（成本）不會太多，但是「啟用率」有保障，現在大部分場景都能被正常啟動。這裡沒有這個投（票）（的坑了）。（做完之後）用戶滿意，然後省了不少錢，很多（真）正的大項目工程（就好了）。

這是第一個例子——想（講）（的）是怎麼通過一個真實用戶非常複雜的需求，（倒）（逼）我們平台去升級架構、去適應。這是我的幾個啟發：第一，隨著 AI 能力變強，我們不要再用「固定的工作流」來限制 AI 了，會變差、效果變差——不要再用 workflow。有段時間（大家）產品很喜歡搞什麼「先（做）設計 agent、（做）分析、然後前端 agent、然後（PD）agent、測試 agent」——不要搞、不要（給）（AI）加約束的東西。最近（流行一個）詞（叫）「駕馭」——就（把 AI）（當成）一個代理：（AI 是）真實世界的（一個）（實體），你（把）（工）具給他、有什麼反饋給他，他想做什麼你讓他（做），你把結果告訴他，他自己來優化、自己決定他的工作——你（只）描述你的需求，這才是最好的。第二點，就是「（長）期記憶」——這是從去年底（Manus）團隊分享經驗給我們非常大的啟發，幫我們解決了很多「上下文記憶卸載」相關的問題、解（決）（了）很多超大（項目）的問題。文件在 Linux 中是一等公民，所以我想說「**文件對於 AI 來說也是一等公民**」——因為 AI 對於（code）來說就是原生掌握的、就是它的原生語言，你（把）（東西）弄成文件，AI 是最擅長的，「文件級」（也）是一個非常好的（架）構設計（理念）（我後面還會再（講））。第三，超大工程管理的一些心得——現在傳統的工程團隊很多以前的工程（積）（累）、（AI）時（代）還是可以用的，非常好，不要什麼（事）（情）都讓 AI 做，有些複雜事情讓（人/工）（程）（工）（具）來做，比如提前的項目編譯這些事情，把工具（提供）（給）AI、讓 AI 怎麼用；以及 **Context 一定要管理好**，不要讓你的 Context 被打滿了——雖然最近他們有（百萬）（級）（的）上下文，但按（我們）的實現經驗，上（下）文塞太多了還是會變（差），這是注定的，它的注意力還是會（衰）（減）——對 AI 來說它只能記得前面幾句話、最後幾句話，（中間）都會忘掉，這是大（模）型的通病、解不了。這是第一個項目給我們的（啟發）。

## 案例二：一群 HR 同事自助搭建「內部流程 & 管理平台」（成為超級個體）

下一個（案例）——一個 HR 團隊的（故事）。這個是想講：如果你們真的想在企業內部落地一個類似的平台，你們不只是要把技術做好、做紮實，你更要把「落地」給（做）（好）——怎麼落、怎麼真的讓非技術群體能用起來，非常關鍵。

（這）群體最早（的）時候，他們就說「你們平台很笨、聽不懂什麼（意思）」。（比如他們用了）（螞蟻內部特殊的）（東西）（模型不懂）——很正常，大（模型）沒有訓練過（這些內部）東西、沒辦法。他說了很多次大（模型）（都）（不）懂。然後那段時間（Anthropic）發布了一個 **Skills**，我們就（基）（於）（Skills）（的）協議、在我們的平台做了一些內（部）支持：我們讓他們（那些）特殊的內部（能力/知識）——我們把它轉成了 **Skill** 的形式，我們（用）（SQL/工程）（讓）（大家）自己維護，然後就像我剛剛說的形式，每次我們（載）（入）一個 Skill 的時候，就把它以文件的形式存在本地，這樣整個 AI 的（設）（備）（生命）週期過程中都能記得這些東西——比如他提到了（某個內部）（價值觀/概念），那就可以給它喚起、就可以撈出（來）。

這裡還想講一點：**大家以後做（權限/集成）的時候有一點要注（意），就是一定要和業界的標準對（齊）**，這樣你們以後天（生）就會享受到生態、工具的發展——比如我們這個（底層）直接換（成兼容的），（它）（就）能跑，因為（協議）（接口）是對齊的。這（是）解決企業內部知識（的）第一（個）點。

第二點是企業內部實際使用的場景：比如他說「做一個員工的搜索器，（我）（按）（工號）搜（索）（某）一個人」。那這個我沒辦法每次都跟用戶說「你要先啟動一個叫做（螞蟻人員）（查詢）的 Skill 才行」——這個不行；就算我把那個 Skill 做成內置的技能，也（會）（效）（果）不好。（所以）首先我們直接在（AI）（家）（裡）給它內置了很多 API、內置了很多（組件）庫。這時候我想說一個（理念）：**給 AI「家裡」（放好東西），（讓）他自己決定什麼（時候）啟動、再把它（鎖）（起）（來）、（再）放文檔**——是這種非常好的「私有知識啟動」的方案，這個比（純）（Skill）的（啟用）率高很多，這個幾乎（百）（分）（百）（了）。（例如）（在）我們（平台）（裡）說「我要做一個員工選擇器」，它就會直接做出這個，而且 UI 可以隨（意）更改——因為我們把 API 給它，它可以用（自己的）UI、也可以不用（只用 API 也是可以的）。

第三個（點）：有天（我）（跟）（那）（個）HR（聊），（他）問（我）「（喬）（楊），你們這個平台的數據安全靠不靠譜？我如果放一些比較敏感的數據行不行？比如薪資這些。」我說「你放心，我們團隊雖然是前端團隊起家，但我們現在（DevOps）（很）專業，保障數據絕對不（洩）漏、（權）（限）（不）（（漏）），你不讓別人看、別人絕對看不到。」他（說）「（那）我的（問題）（是）：你（本）人（能）看到（嗎）？」（這一下）我幹不了——我是平台的超（管），我也是平台的開發者，我肯定能看到呀。他說「不行，那我用不了你平台、我就是要放（薪資），我（要）（求）（誰）都看不到。」他這事當時（給）（我）很大很大觸動。（所以）我們設（計）（了）兩個功能：一個是「**測試級 DB 和正式級 DB 的區別**」——你在對話創建這個網站的過程中，我們都是測試數據庫，這時候可以看（薪資），（因為）（是）假的；然後上線了正式（數據）庫之後，我們單獨設計一個權（限）角色叫做「**數據庫管理員**」，這個和「開發管理員」是分開的。（其實）不是說這個技術（多）（難）（做），（是）（把）（平台）內部可能會遇到（的）（一）（些）問題大家可以提前準備。

最後一個是「內部生態」的問題：他說「我這個數據能不能和我們內部的（某）（個）平台打通？我們內部（有）（文）（檔）（平台）、還有（其他）平台，我能不能隨時從那裡讀、（往）那裡寫？以及我能不能給員工發通知、發郵件、建日程？」——用戶說什麼都是對的。那作為（我們）（就）做了很多（方式）來把我們的平台連接上內部：我們（可以）（發）通知、可以連內部文檔等等。所以這是我另（外）（想）說的觀點：**（Vibe）Coding 的平台在內部落地，一定要做「一站式全包」，而且連接內部所有（工具）**——（很）（大程度）（是）（因為）（合）（規）。要不然（想想）：（為什麼）不用外部的平台呢？就（是）（因為）合規嘛。（但光合規不夠，）一定要（讓）（它）（真）（正）好用，一定要真的把他的工作流可以真的搬到你的（平台）上來才可以。

## 案例三：一個營銷（活動）的例子（面向 C 端大流量）

OK 這是 HR。最後再講一個營銷的例（子）。有天跟我一個比較熟的營銷（同事），他跟（我）說「我最近（有）一次要做一個營銷活動，（能不能用）你們（這）（個）平台？」我說「你（沒）（有）開發（能力）（也）行——你來試試，我們這個平台平時都是做內部產品，從來沒有做過大流量的外部 C 端產品，你來實驗一下有什麼（問）（題），我跟你貼身支持一下。」

OK 第一個（他）說「我（有）（設）（計）（稿）——跟你們之前那些（平台）不一樣，我們有設計稿的，我花錢（做）（了）設（計），（你）（們）（一）（定）（要）（還）（原）。」那傳統的方案怎麼做？我們（以前）做 D2C 非常專業（，有很多 D2C 方案）：比如你（是）（Sketch）（設計稿），我們有一些 D2C（方案）；比如你如果不是這種結構化（的）、是（圖）片，我們當時（用）通（用）模型都轉成一個中間（DSL），再通過我們（主）的 coding agent 做 React。這是當年的（做法）。就最近才發現這（些）（都）（不）（用）（做）（了）——現在多模態模型太強了，我們現在（幾乎）不（用）（做）（D2C），（直接用）多模（態）（模型）的（coding）能力就已經很強了。所以這（個）變（簡）（單）（了）：他這個需求只需要我們做一些約束，讓 AI 自己根據（設計稿）還原，再根據實際的（瀏）（覽）（器）截圖反饋、告訴他哪個做對（了）、（跟）他差多少，再接（著）改、跑一個循環上去就好了——就（很簡單）地解決（了）（這）（樣）（的）問題。

（第二個）他跟我說「（這）和做普通的外（部）頁面不一樣——這個做的是我們的（支付寶）客戶端（裡）一個特殊的頁面，這個頁面其實有很多獨特（的）能力，這些東西是 AI 不懂的，那怎麼辦？」這個方案我之前講——我們有一個 Skill 的方案，但是 Skill 的方案還是不夠完整。我（更）想推薦那個（refer 方案）：這個（方案）特別好用。我最（常）用就是「**把你的（家）裡（面）直接內置一些例子（examples）**」——比如叫 examples，把（例子）給它，對於大（模型）來說（例子）是最好學習的；然後在你關鍵的地方放上（索）引，就是說這個（Agent）你可以查找到、你（改）（時）（去）（看）一個 **refer 文檔**——這個文檔一定要是一個「對大模型好的」文檔庫。我們內部現在很多文檔庫都已經改成這個模式（**llms.txt**）——大家應該知道，很多網（站）都（是）（這）（個）格式，這格式裡面就是一個 Markdown（大）（綱），它可以直接消費、直接搜索、（甚）（至）（下）（載）（到）本地（後）（續）可以用。所以這個方式對大（模型）來說是一個「漸進式」的：它可以直接在你的（項目）裡面按照你的模式（寫）（代）碼、也可以看你的例子（看看）在（支付）（網）（絡）裡面怎麼發起（請）求；當（有）（某）（些）特殊的（客戶端）功能（比如喚起攝像頭）他不知道怎麼用了，給他一個 refer。這個模式對於「私有知識 AI 驅動」是非常有效的，這是我們的經驗。

再一個是他說「你們做的東西發到外網之後，能不能（承）（受）大流量、（高）（並）發？」——這其實是個很經典的問題。但實在（是）我們內部很多（技術）（同事）都在提要求（因為我們做內部平台的內部產品有很多人用、得到很多人喜歡，但他們想做外面大流量），我們當時（一直）（說）不行、後來這（種）（需求）越來越多了，覺得還是（得做）。這個是什麼呢？（就是）叫做「**（ASE）**」——（Agent）寫（代碼）寫得快，然後我們平台保證它「發得快」、保證它「非常安全地發布」到（我們）內部一個非常（高）（可）（用）的（部署）平台上。我把它（吹）（成）（「）敏捷（」）三個快：（但）這三個（快還吹）（一）（個）——「**寫得快、發得快、跑得（好），（可）（能）掛得快**」——這是（提醒）（，）（不是）（（（（（（（（。

（另外）如果（有）（人）想（一）（個）觀點——就是大家很多 FOMO 情緒，覺得（AI）時代程序員是不是會被（取代）？我覺得不會，我覺得真正高級的程序員的價值只會更高。（比如：）AI 能幫（醫生）（做）（手術）嗎？（幹）（不）（了）；AI 能幫財務（做賬）嗎？（幹）（不）（了）（負責）（（。（回到）（這）（個）——高級（程序員）會知道怎麼能更好地駕馭越來越強的大模型，（其）（實）（是）高級（程序員）（選手）才會用得更好、才能做更好的產品給大家用。我們這裡做了很多手段，比如：首先有一個「**自動構建測試**」（這個很好理解——大（模型）每次（生）（成）的代碼，我們（叫）（LP/CI）都會自動給它跑一遍 lint 這些東西，跑完之後如果有問題，我們直接會在（對話）裡面直接告訴他、直接反饋，它根據我們的反饋再（改）就能（改）好）；如果修不好、（運）（行）時（報）錯（runtime error）怎麼辦？我們在剛剛我演示那個「對話 + 頁面」裡面，所有（運行時）的報錯我們都會收集下來，然後（給）用戶提供按鈕叫做「**一鍵修復**」——哪個頁面、哪個按鈕、（點）的時候是什麼狀態、然後報錯了，告訴 AI、AI 會修（我們還會有監控）；另外是「**離線任務掃描**」——這個針對（用戶）如果寫了一個網站、他自己沒有把所有頁面都測（的）場景，我們會（在）（線）（上）每天晚上定（時）（在）某個時間把他（的）作品每個頁面點一遍、每個（功）（能）（用）一遍，如果報錯了（就）（趁）（AI）（不）（（（（，把代碼（修）好。這是我們做的一些手段。（未來還有很多。這（就）（是）（為什麼）我覺得（AI）時代程序員（的）價值（更）高。）

## 一些額外的經驗與思考

**（做普惠、低門檻）：** 我覺得在 AI 時代做 AI 產品，我們是要（做）更多低門檻的場景、然後讓更多人使用——是要做「普惠」，不要做「面向專（業）（開）（發）（者）」，很難做。我們曾經做過很多面向我們內部的前端、後端、算法（的）工具，但他們選擇太多了——他們可以用 Claude Code、可以用 Codex、可以用（Amp）、可以用 Cursor，他們沒有必要一定要用（我們）內部的產（品）。我覺得真的要有一個「AI 的放大效應」，一定是要做「（普）（惠）」、（讓）（更多）（的）（人）（用）（上）——他們（本）（來）沒有研發資源，你的平台落地可以產生更大的價值、可以放大（效應）、可以服務更多人。這是最近一個數據：我最近一個月（不）（是）（說）5000 人用，這 5000 人真的（在）敲代碼、創建了東西，然後大概也花了 600 多億（tokens）——這其實（也）很危險，頭部（重）（度）用戶花特別多。

**（多形態與跨 Agent 協作的難題）：** 還有一點，AI 產品到後期、用戶（摸）（熟）之後，會面臨「每個用戶的需求非常不一樣」。我們的 PD、設計類的崗位，他們平時的工作流是想要做文檔、做表格、做 PPT，這時候我們給他做了一個（場）（景）（在）（這）（裡）（對話）（就）（可以）（直接）（做）；（我們）（分化）（出）（了）（五種產品形態：文檔對話、生成通用網站、做海報/創意營銷類等。）但是又遇到了新的問題——用戶需求非常多，他發現平台已經做這麼多了，「那我做了這裡的海報，能不能在（網站）裡用呢？我在這裡做 PPT，能不能在網站裡（用）？」我們現在（的）（做法）（是）（基）（於）（一套架構）（往上長出很多能力）；但我們的 agent 現在（是）獨立的，所以它（這）（個）（協作）很難、怎麼辦？這個問題我最早做了很簡單的一個（版）本，比如「文檔可以幫你轉圖片」——我們有個生圖的 agent，（分析）文檔（覺得）應該配幾張圖、每張圖描述（什麼），那我可以（把）文檔轉（成）圖、圖（可以）轉代碼……我提供 AI（信息）（在）（Agent）（之）（間）（傳遞）（的）（能）（力）。但這（個）回到了我之前說的、我覺得不是很好的（一）（條）（路）：**我覺得多（個 Agent）之間如果是用「文本」的（形）（式）（傳）（遞），（損）失度非常高**——對於 AI 來說，AI 本來就是「（全）（知）（全）（能）（的）（原）（生）」，你讓它用「上下文（傳遞）」（或者）（換）（角色），把它的意圖壓縮成一段文本、（再）讀了非常多內容，非常不好，而且這個（成）（本）（也高）。後來我們最近在做（一）（個）（事）（情）：（就是）（有）（一）（個）（大）（的）（通）（用）（權）（限）（的）（Agent）（直接操作所有內容——但其實這個有問題（就我們剛說的）：每個項目（的倉庫不一樣、存儲的元信息不一樣、裡面的文件（結構）不一樣，甚至版本（管理）都不一樣）……

（——第二段——）

……（多人協作）：他們在很多真實的項目裡面都在（用）（Muse）（做），比如一個 PD（帶）五個人，他們一起在做一個項目、又要做圖片又要做文檔又要做網站，他們沒法實時多人協同。傳統的軟件協同方式（比如）（分）支合併，這個方式不太適合（非）（技）（術）群體——（你要）（讓）（他）（在）這種方式（下）（玩），你告訴他「你要先啟動一個新的分支、在分支上做一些操作、然後再合併回去」，你要告訴他分支和分支的關係、告訴他什麼是 revision、什麼是（（，不行。所以這兩個是最近面臨的很大的挑戰。

**下一代架構的三個理念（多個 session 在一個 workspace 跑）：** 最近我給我（團）隊提了三個架構理念。**第一，一切內容都是文件**——文件才是（AI）（時）代的一等公民（我剛剛說了一遍，因為文件也是 Linux 的公民，所有的 bash 都是圍繞著文（件））。把剛剛的圖片、剛剛的 Excel、剛剛的文檔，全部都（變成）文件，這樣 AI 就可以跨倉庫操作所有類型。**第二，一切文件都要用 Git 管理**——我們之前每個圖片、每個數據庫、每個文檔都是有自己的（存儲）、自己的元信（息）管理，（那）（就）很（麻）（煩）：我如果一個文檔、一個 React 項目、一個 H5 項目放在一起，三個不同的版本管理，對工程壓力非常大（，（（（。（比如）我一個回（滾），我還要確定我這次回滾是（回滾）文檔、還是回滾代碼、還（是）（回滾）（（。所以一切文件都要（用）同一套 Git——（這）（對）大模型最擅長：當你想問「我的項目之前這裡有個功能為什麼弄丟了」，那大模型會自己（用）命令去 `git log` 找到（某次提交）（比如）我和另一個同事合併之後東西丟了、或哪裡壞了，大（模型）可以自己去找 Git（歷史）（處理）。**第三，一切會話都在 worktree 裡面**——這個是非常重要的、（用）（Git）（worktree 的）方式（（。現在（新）的模式都是 worktree 模式：（比如）本地 Claude、那個 OpenClaw（下），你就會發現 OpenClaw 那個作者在本地寫了很多很多關於「AI 怎麼用好 worktree」的（方案）——因為他本人開發項目的時候本地起了非常非常多（並行）實（例），為了讓每（個）（Agent）能在不同的項目、不同的目錄裡面工作好、還能把工作匯（總）（回）（來）。

所以這三個原則，就是我下一代（Muse）（的）（設計）——（讓）（統一的）（AI 使用）：一切都（對）（我）（要）（的）（AI）（友）（好）。這也回到我之前說了好幾次的理念：**不要發明私有協議、不要發明自己的各種（私有）（接）（入）工具，一切都要按照開源協議兼（容）的方式**，你可以增強、可以漸進式增（強）——就類似於那個 **Obsidian**（他們（有）（個）（MD）（模式）：（Code）（這個模式）好用，就他們基於 MD 的基礎擴展了自己的一套（連）線）。這是我講的、可能是這個時代我們設計（的）一個理念。

**再往後我還有幾個觀點（或者叫「暴論」）：** **未來的基（建）必須（原）（生）（AI）**，因為以後的基（建）不是給人用的、不是給開發（者）用的，以後所有的基建都是給（AI）（用的）。第一點，我覺得未來「企業或者自己團隊造（輪）（子）」的時代已經徹底過去了、這個窗口已經徹底關閉了——大家（如果）有（存）（量）程序（員），絕對不要在自己企業的內（部）（造）（自）（己）（的）（框）（架）、自己的（東西），不要造！只要你的東西 AI 不懂，這東西（就）用不起來——以後你造一個新的框架，比如現在 React AI 都會寫，你再造一個新的框架、AI 沒有學過、AI 不會寫，那（就）寫不（了）、（不）（會）（用）（了）；現在很多程序（員）（會）（的）（古）（法），變成（（（AI 不會用（（。所以第一點：**任何東西都要保證 AI 會寫**。那你（企）（業）內部真要做一個最佳實踐怎麼辦呢？就是我剛說的：你保證你的編程界面和外界的開（源）方案對齊——比如我剛（介紹）了我們數據庫的（能）（力）（服）（務）化的方案，我們參考了 **Supabase**、我們和它的每一個 API 都完全對齊，我們就告訴大（模）型說「我們這是 Supabase」，他只需要換個 endpoint、換到我們內部的這個 DB 服務上去，整個它（的）體系就跑起來了。

第二個點，**未來任何東西都要保證 AI 會用、人類不會再讀文檔**——這是我（的）觀（點）。一切文檔都是寫（給）（AI）（的）：你們可以回想一下，你們最近一次看 API 文檔（是）什麼時候？（很久了）（吧）——（都是）（把）API 文檔給 Claude、讓 Claude 寫（。（既然）（（，人為什麼還要看 API 文檔？所以我覺得所有的基礎能力，我都必須畫（成）（IaC）（infrastructure as code）——把所有的 infra 能力就變成可以（配）（置）（的）code。（包括）以後所有的框架、所有的（組）件，你們要確保自己的能力一定有一份「對 AI 非常友好的文檔」。

第三個觀點，是「以後的流量入口（和）（CLI）（化）」——這個（可能）（超）（（報告（範圍）（，這個我覺得大家都認同。這個點背後有一個思考：**以後每一個平台都要想清楚，你自己是「流量入口」還是「工具」**。你如果不是流量入口，你可以不用（好）（好）做你的 UI 界（面），因為用你的網站的可能不是「人」而是「AI」——比如最近（釘）（釘）在做 CLI 化、比如「悟空」其實就是這樣。他只要不是流量入口，怎麼樣讓他的能力被別人的 agent 所使用才是重點。未來每個網站都要維護好自己的（文檔），不論是叫 llms.txt 還是 refer for agents（這是（那個）（（book/某社區）（（他們的規範）——這種文檔才是網站以後的關鍵：把你的（網站）能幹什麼、你的（API）描述寫清楚，讓別的 agent 可以驅動；並且（要）設計一套「能給 agent 發身份、（鑑）（權）」的系統。這個我們內部現在很多體系已經開始（建）（起）（來）（了）——螞蟻內部已經有一套專門針對 AI 的身份（鑑）（權）系統。以前我怎麼做？以前為了把我個人的所有工作都搬到我的 Claude（上）（去）驅動，我把我個人的 cookie 寫到了我本地（，我（有）（個）（腳）（本）我每天定時更新，有段時間被安全（找）（了）——安全說「（我）看你那個身份怎麼每天這麼多平台幹這麼多事情？」我說「是我的 AI 幹（的）」，他說「不行」——這種 cookie 的方式（（（，畢竟 cookie 還是（明）（文）（（，（（大模型（用）（（比較危險。所以要設計一種新的、針對 AI 的 **SSO 登錄**，這是很好（的）點，我們最近（動）（得）比較快，這裡（螞蟻）已經（成）（功）落地了這個事情。

這個是我覺得**未來的所有軟件，如果不能 AI 驅動，就（會）（被）淘汰**。我寫這個觀點時候微軟還沒有出他那個叫 **MarkItDown**（（Mket）——是最近我看那個（GitHub）trending、應該是這個月才上的，它裡面就是可以把 Microsoft 所有的軟件（比如 PPT、Excel）（這些）（東）（西）（轉）成 Markdown。為什麼是 MD？因為 **MD 才是 AI 時代的原生的第一語言**。所（以）（我）（的）（判斷）（是）：未來（（（所有不能被 AI 驅動的軟件都會被淘汰。比如以後你們（企）業內部出一個「通用的 AI Agent 工作台」，它可以（接）（管）你的所有產品、（需）（要）（的）聊天、（需）（要）（的）設備管理、（需）（要）（的）（所有東西）；但是有一個軟件（接）不了（比如（某）（個）），那你可能會（（（——因為「（（人（變）（（（這件事情是不（（（，（（如果幫我幹事（（（會（（（（。這是我（剛）講（的）為什麼很多軟件（（。今天還有一個很火的項目叫做（（（——也是一個理念，（把）你的軟件更快地變成 CLI。我們（企）業內部現在有很多很多軟件都變成了（CLI）——（CLI）（是）（AI）（的）原生語言，你給他加一個 CLI（，對（B）（端）（場）（景）來說非常好用，它可以自己組裝它的（（（。

還有一（個）（點）：**交付才是核心，以後 CLI（（編）（碼）（（是最不值錢的事情**——對任何（（（來說（（，（（（大模型（（（（，coding 根本不是什麼核心問（題）；核心是「真正怎麼讓你的用戶在你產品上真正地把他的工作流搬上去、把他的需求解決」——這就是我剛前面講的第二、三個（意思）。（（（（太簡單了，能讓他「一站式」地在我們的平台上把他的工作完成，他才會用這個平台，不然他（（選擇太多。這（是）第一個「交付才（是）（核）（心）」。

（你會想：各位如果是程序員，你會想你每天寫（代）碼的時間（有）多少？我感覺可能（也就）（不知道）（（兩個小時，每天（（都在開會、（（本來（（（（比較多。所以「（（（跨平台」（（才是難題——這（是）（第）（一）（個）（觀）（點）：AI 產品要把各種平台打通、把（交）付做好、把（落）（地）（做）好。第二是（（（，未來（（（（什麼意思呢？就是人們不會再關注「你這個功能是底層跑的（（）實現、還是通過（（（實現」——（（（如果你這平台（token）用完了，就等於「關機」了。我們現在很多我們公司很多同學，他那個 Claude Code 那個 limit（不）（小心）（（（，（（就）幹不了活了。（（（

想講的就是：作為一個架構師（身份）跟大家（分享）——**任何架構都要「面向變化設計」，任何事情在這個時代都有可能在三個月內發生巨大的改變**。我們這個（（（（改了好多，但是因為有些東西是「天生面向變化設計」的，（（（成本沒有那麼高。我就（想）跟（大家）說：大家如果做這個產品，一定（要（做好準備）——（（可能每三個月就（要）改變一次。

然後（分享）（一）（個）個人經驗的（進化）吧：最早做（（（是為了「釋放大模型的潛力」，但（（（（要拉住它；然後有段時間流行 spec（就是 OpenSpec），我們也試了，但我覺得 OpenSpec 就是給他加個約束、本身就是（harness）engineering；然後下一個怎麼做 TDD？（（（我們給它生成一套 test，那 test「確定性的執行」來約束、給他結果——還是（（（（因為 test 生成也不准，這裡（（（。所以想說：**沒有一個固定（範式）（是）最好（的）**，大家只能說「我們如何在真實的環境中讓你的 AI 產生（（（運行、讓他自己覺得他需要做什麼、驗證他需要什麼、代碼（（後的結果怎麼反饋給他」——這（就）（是）（最近）**Harness** 的觀念：怎麼把真實世界給他、把（（（（錯（（給他，讓（他）自己（訓）（練）。

**（行業趨勢分享）：** 隨（便）（（（（幾個（最近的）熱門項目（（（分享（（一些行業趨勢。（第一，）（MiniMax）（（（——比如 MiniMax 做了一個（（Max（（（，他（（（（跟（Anthropic）（（（，他有三點：第一點，他每次（（完（（（，他會自己決定是不是應該沉澱 Skill；第二，（（（時候會判斷這個 Skill 過（時）了、（（（要（（（（；第三個，是每（（（次（（（（，它會（（（（一下「我這次是不是應該（（（」——所以這個是一個「自進化」的路（。這個我聯想到（（（（的時候，你可以看我這裡（（（（一個 research（（（概念，它一共三個文件（train、prepare、（一）（個）program 的 md）：人類只寫 program 的 md，AI 只寫 train——就是說人類設定一個目標，AI 自己決定每五分鐘一次迭代，這（（（benchmark 上升了、（（（（我就（保）（留）（（（，都是一個「自進化」的（（（。這兩（個）項目是我覺得未來的趨勢吧——一定是（往）「自學習、自（進）化」的方（向）走。（（第二個（（（是上個月（（推（特）（上）的熱門，就是分享一個「他如何管理 LLM（的）知識」（（——我（（想說（（（（就是「RAG 不好用」（我不知道你們有沒有用過 RAG，（真）（的）（不）（好用）（（（），所以管理知識（（（更難，因為（（（文件管理面臨（（（問題（（就是文件太多了之後怎麼辦？（（（他們提出（（（是（wiki）方式，把（（（（（鏈接起來（（（就變（（快，（（（（這個（（（（也是非常（（（（。

（（（（（最近（（（（在（Claude Code）的場景下怎麼給他合適（的上下文）、讓他幹活幹得更好、別亂改——所以這（邊）（給）（我）（的）（一個）啟發（（（就是：**「釋放（AI）（的）潛力」和「駕馭（約束）（AI）（的）行為」這個事情其實是「雙軌」的、而且會（（（（發展**——你一邊要釋放（AI 讓）他做得更好，一邊要約束他、（讓）（他）按你的預期來進行。（（（隨便講（了）（（（幾條吧，感（覺）（（（（比較（（（（分享（（（。

我今天的所有分享就結束了，感謝（（各位）（老師）。

---

## Q&A

**主持人：** 我們先感謝（彭）（老師）的分享，然後學到很多（好）（的）內容。我看到最後（您）分享（了）一下關於 coding 的一些思考，從 coding 到 Spec-Driven、到（（（Driven、到最後的一個（（（Driven。我可能對於最後您講的那個 Harness 想（多）（了）解一下——因為現在 Harness 有很多不同的定義，包括 Anthropic 它有一個（（（（（（（自主的一個系統，然後想請教一下：從您的（（（（角度，這邊的 Harness 是什麼？有哪些實際的落地經驗？

**彭佩喬：** 問題特別好，這是我們團隊最近正在追求的事情，我們有一個團隊直接改名叫做「Harness 團隊」。Harness 是什麼呢？最早（是）（Anthropic）提（的）概念，它有個最大的共識是「**模型 + Harness**」（（（。它是個特別（（（（的概念，它並不是一個固定的範式、和之前 TDD/SDD 都不一樣。它的理念就是我剛說的：**給模型真實的運行環境**——給模型工具，也（就）（是）（讓）他跑在一個「雲沙箱」上，給他 Python 和編譯（（），他想要執行任務（比如幫你完成一個網站），那麼他在完成的過程中，它可以直接調用（（（process 來判斷這個代碼（能）（不）（能）跑起來，它可以自己（（（（去看他的 UI 好不好，他自己完成這件事情、自己獲取（（），最終把事情（（（（（完（（——就還是我剛那句話：**在真實環境讓 AI 能夠「驗證 - 反饋」循環、並且能（（持續（（（（（**。我覺得這個（（不新（（，但我覺得這個（範式）應該是（（進化（的）——它可能不是一個固定的一句話。

**主持人：** 下一個問題。

**觀眾 2：** （彭）老師好，（像）這樣的低代碼平台或者無代碼平台一個很重要的問題，就是用戶它不可能（自己）去進行 debug 的。所以你們是怎麼樣保證你們產出的這些東西，它在第一次運行的時候（就）能夠給用戶一些反饋、（不）帶著（報）（錯）狀態？你有沒有什麼措施？

**彭佩喬：** 這是我們在去年 7 月份的時候遇到的最大的（難）題——當時的模型能力還不夠強，它的首次（成功率）（（（在（（）？我們當時判斷成功率只有 80%（他寫完一次性通過就 80%），非常難搞。我們當時（（（了）一些方案，比如我們有一套（（（（，（基）（於）（那）（個）（Vite 之類的）（（（（，它的前端跑起來之後，我們（（）拿它的 build 報錯、runtime 報錯，然後（（）（相當於）用戶（沒）（看）（到）（（（（代碼（（（（（因為報錯了、然後自動我就可以（（下去（（（——這第一種，我們當時的結果是我們把首（次）（成）（功）（率）打到 99%（這是我們大概努力三個月後的結果）。今年我們（（（（後來切到 **LangGraph** 之後，我們（（）（在）（LangGraph）（（（加個節點：在代碼 coding 的節點結束之後，我們直接就在 agent（（（裡面直接給它做 lint、還有 test（（），保證它出來（的）代碼已經是好的。這（（（（做到（（（（（，（（我覺得以後還會有更多（（（。

**觀眾 3：** （彭）老師你好，（（（（（（（因為我不是程序員、我是做產品（的）——其實就想問一下，就是你們這個項目、產品（經理）的同學在這個過程都做了什麼？就是未來產品（經理）他要去跟 AI、（相）（當）（於）（（（類似的這樣的項目去結合的時候，產品應該去怎麼（走）？（（可能他的一個發展方向是怎麼樣的？

**彭佩喬：** 這個話題在我們（（（（內部最近討論的特別多：「產品經理以後在（AI）時代應該是一個什麼樣的工作流、怎麼樣和（工）（程）（（（去配合」。首先，我們（先）（（（一個「傳統的（（（（需要」——我們已經沒有（（（（（（（（，（（（（（（（（（（（（（（，我們也沒有（（設計、（（（交互（（（（了。（（（（我們（產品）（裡）（（（（（（給我們（（直接是 demo：他想做什麼呢，他直接用我們的平台做 demo 出來。那有時候（老）（闆）在問「那為什麼還要有產品經理這個崗位？這（個）崗位到底核心是什麼？」我現在回答很明確：**產品經理就是「（離）（用）（戶）（最）（近）」的（人）**——他知道我們用（戶）到底要什麼功能、他來引領（（（（我們（（（（非（技術）群體到底關心什麼東西、我們產品下一步（怎麼）（（（（。我覺得這個是非常關鍵的。我們團隊最近確實也在討論、（（（（不（（（（（（（（這個答案（（（。好，時間問題，我們（（（提問就到這（（。

---

## 主要校正對照（原稿 → 校正）

- 彭橋 → 彭佩喬（花名喬楊）；團指書寶體驗記錄部 → 螞蟻集團-支付寶-體驗技術部；畫面/敲陽 → 花名/喬楊
- 平台名 = **Muse**；ing 平台 → coding 平台；不克平台 → Muse 平台
- API 網關；出入攤 → 出入參；圓信息 → 元信息；reaction → React；月電機 → 預覽鏈接
- C engineering → Context Engineering；流失染 → 流式渲染；略博/MAU；12 人 → 12 輪
- PO 同事 → PMO 同事；戰役管理平台（2000+ 輪、6 億 tokens）
- S 工具 → Search/Replace 工具；gget patch → git patch（80%）；行號（90%）；aging loop → agent loop；narrow 收窄
- cash → Cache；KV 潛/KB 潛 → KV Cache；ic → Anthropic/Claude（Prompt Cache 5 分鐘/1 小時）
- system char → System Prompt；readwite → read/write（tools）；ple map/codindex → repo map / code index；curs → Cursor；opened files；user message
- 山斗人 → GitHub 上某壓縮 prompt 的火項目（待核對）
- 上下文壓縮三層：memory / user message / 文件系統；man/Manus → Manus（文件即記憶）；WB search → WebSearch；記憶卸載；skills；coding → CLAUDE.md
- MCP complex → MCP 結果外置的火項目（省 90% 上下文）；getcus user info → getCurrentUserInfo；有善無環圖 → 有向無環圖（DAG）
- GY → GUI；SAS SDK/Sabase → Supabase SDK；ol → 一套 tools；snapc → snapshot；modifym → modify message
- lal storage → localStorage；prumping → prompting；螞蟻人工的 s → 螞蟻內部 Skill；鍵左鍵庫 → 組件庫/API 庫
- 測試級 DB / 正式級 DB；數據庫管理員 vs 開發管理員
- 影銷 → 營銷；DPC → D2C；skat → Sketch（設計稿）；p 政府 → 圖片（非結構化）；rect → React；多摩態 → 多模態；知網客戶端 → 支付寶客戶端
- Sill/錄/referrefer → Skill / refer 文檔；examples；lms.txt → llms.txt；換起攝像軸 → 喚起攝像頭
- ASE / AS 代 → ASE（Agent 寫得快 + 平台發得快 + 安全發布）；敏捷三快（寫快/發快/跑好，可能掛快）；limit → Claude Code limit
- fall 情緒 → FOMO；自動構建測試/lint；一件修復 → 一鍵修復；R 爆錯 → runtime error；離線任務掃描
- worpace/W → worktree / workspace；revis → revision；git log；Osidan → Obsidian（MD + 連線）
- 基建必須原生 AI；IaC → infrastructure as code；llms.txt / refer for agents；悟空（螞蟻桌面工具）；SSO 登錄 / AI 身份鑑權；cookie
- market/Mket → MarkItDown（微軟，Office 轉 Markdown）；MD 是 AI 原生第一語言
- logar/logarp → LangGraph；wriner → Vite（？）；首次成功率 80%→99%
- Q&A：Harness 團隊、雲沙箱、驗證-反饋循環；產品經理=「離用戶最近的人」

## 仍不確定、建議你再核對的詞

- **GitHub 壓縮 prompt 項目「山斗人」**、**MCP 結果外置項目**、**CLI 化項目「toi」**、**Vite/構建工具「wriner」**、**Obsidian 的「MD + 連線」**：多處火熱項目/工具名發音破碎，依語意與領域推斷，建議核對。
- **行業趨勢兩項目**：MiniMax 的「自進化 Skill」機制、以及「research（train/prepare/program.md，人寫 program、AI 寫 train、benchmark 驅動）」——名稱與細節依口述還原，建議核對。
- **ASE**（平台快速安全發布能力的代號）、**Harness / Anthropic「SLC 自主系統」**：待確認。
- 第一、二段之間有明顯銜接（多形態與跨 Agent 協作 → 多人協作），中間可能有少量遺漏。「（括號）」處為缺字或依語意/簡報補足；本篇主幹（Muse 平台、三案例、KV Cache、壓縮三層、文件即記憶、三大架構理念、未來觀點、Q&A）已由 34 頁投影片與內容確認。
